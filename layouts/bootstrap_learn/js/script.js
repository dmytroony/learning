$('.carousel').carousel({
	interval: 3000,
	// keyboard: false, // use left - right
	// wrap: true, // stop slider when the last img
});

// $('#exampleModal').modal({
// 	keyboard: false, // Esc, etc.
// 	backdrop: "static", // close modal to press on behind of modal
// });